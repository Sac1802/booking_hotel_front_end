import { Route } from "@angular/router";
import { BookingDetails } from "./booking-details";

export const bookingDetailsRoutes: Route[] = [
    {
        path: '',
        component: BookingDetails,
    }
];