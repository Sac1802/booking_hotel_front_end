import { Route } from "@angular/router";
import { HotelBooking } from "./hotel-booking";

export const hotelBookingRoutes: Route[] = [
    {
        path: '',
        component: HotelBooking,
    }
];